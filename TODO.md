# TODO

- Decide where to surface the package dependency graph generated by
  `scripts/generate-dep-graph.sh` (CI job "go" uploads it as the `dep-graph`
  artifact for now). Candidates: `docs/INTERNALS.md`, the GitHub Pages site
  (`.github/workflows/pages.yml`), or a dedicated CI summary/comment.
