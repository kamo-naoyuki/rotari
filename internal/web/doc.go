// Package web builds Web-facing projections of Rotari state.
//
// It loads persisted model and state data into JSON-ready structures for the Web
// UI, including runs, jobs, attempts, timelines, origins, and display times. It
// should present state for the UI without changing queue or run semantics.
package web
