name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.goos }}-${{ matrix.goarch }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: linux
            goarch: amd64
            wheel_plat: linux_x86_64
          - goos: linux
            goarch: arm64
            wheel_plat: linux_aarch64
          - goos: darwin
            goarch: amd64
            wheel_plat: macosx_10_12_x86_64
          - goos: darwin
            goarch: arm64
            wheel_plat: macosx_11_0_arm64
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install JavaScript test dependencies
        run: npm ci

      - name: Check web asset formatting
        run: npm run format:check

      - name: Install gotestsum
        run: go install gotest.tools/gotestsum@v1.12.0

      - name: Test
        run: gotestsum --format pkgname-and-test-fails -- ./...

      - name: Build
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: go build -trimpath -ldflags="-s -w -X main.version=${GITHUB_REF_NAME}" -o "rotari-${{ matrix.goos }}-${{ matrix.goarch }}" ./cmd/rotari

      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: rotari-${{ matrix.goos }}-${{ matrix.goarch }}
          path: rotari-${{ matrix.goos }}-${{ matrix.goarch }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Bundle rotari executable into Python package
        run: |
          mkdir -p python/rotari/_bin
          cp "rotari-${{ matrix.goos }}-${{ matrix.goarch }}" python/rotari/_bin/rotari
          chmod +x python/rotari/_bin/rotari

      - name: Build platform-specific wheel
        working-directory: python
        run: |
          python -m pip install --upgrade setuptools wheel
          python setup.py bdist_wheel --python-tag py3 --plat-name "${{ matrix.wheel_plat }}" --dist-dir ../dist

      - name: Upload wheel
        uses: actions/upload-artifact@v4
        with:
          name: rotari-wheel-${{ matrix.goos }}-${{ matrix.goarch }}
          path: dist/*.whl

  release:
    name: Publish release
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download binaries
        uses: actions/download-artifact@v4
        with:
          path: dist
          pattern: rotari-*
          merge-multiple: true

      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "${GITHUB_REF_NAME}" dist/* --repo "$GITHUB_REPOSITORY" --verify-tag --generate-notes --title "${GITHUB_REF_NAME}"

  update-homebrew-formula:
    name: Update Homebrew formula
    runs-on: ubuntu-latest
    needs: release
    steps:
      - name: Check out Homebrew tap
        uses: actions/checkout@v4
        with:
          repository: kamo-naoyuki/homebrew-tap
          token: ${{ secrets.HOMEBREW_TAP_TOKEN }}
          path: homebrew-tap

      - name: Update Formula
        working-directory: homebrew-tap
        env:
          VERSION: ${{ github.ref_name }}
          REPOSITORY: ${{ github.repository }}
        run: |
          url="https://github.com/${REPOSITORY}/archive/refs/tags/${VERSION}.tar.gz"
          sha256="$(curl --fail --location --retry 3 --silent --show-error "$url" | sha256sum | awk '{print $1}')"
          sed -i -E \
            -e "s|^  url \".*\"$|  url \"$url\"|" \
            -e "s|^  sha256 \".*\"$|  sha256 \"$sha256\"|" \
            Formula/rotari.rb
          git diff --check
          git diff --quiet && exit 0
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Formula/rotari.rb
          git commit -m "rotari ${VERSION}"
          git push

  publish-conda:
    name: Publish Conda package (${{ matrix.subdir }})
    needs: release
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-latest
            subdir: linux-64
          - runner: macos-15-intel
            subdir: osx-64
          - runner: macos-14
            subdir: osx-arm64
    defaults:
      run:
        shell: bash -el {0}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Conda
        uses: conda-incubator/setup-miniconda@v3
        with:
          miniforge-version: latest
          activate-environment: conda-build
          channels: conda-forge
          conda-remove-defaults: true

      - name: Install packaging tools
        run: conda install --yes conda-build anaconda-client

      - name: Build package
        env:
          ROTARI_RELEASE_VERSION: ${{ github.ref_name }}
        run: |
          export ROTARI_RELEASE_VERSION="${ROTARI_RELEASE_VERSION#v}"
          conda-build conda/recipe --output-folder dist

      - name: Upload package to Anaconda.org
        env:
          ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_API_TOKEN }}
          ANACONDA_USER: ${{ github.repository_owner }}
        run: |
          test -n "$ANACONDA_API_TOKEN" || { echo "ANACONDA_API_TOKEN is not configured" >&2; exit 1; }
          anaconda -t "$ANACONDA_API_TOKEN" upload --user "$ANACONDA_USER" "dist/${{ matrix.subdir }}"/rotari-*.conda
